const { Topic } = require('./Topic');
const { Book } = require('./Book');
const { User } = require('./User');
const { Upload } = require('./Upload');

module.exports = {
  Topic,
  Book,
  User,
  Upload
};
