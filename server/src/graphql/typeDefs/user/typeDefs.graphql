type User {
  _id: ID!
  name: String
  email: String!
  createdAt: String
}

type Mutation {
  login(email: String!, password: String!): String
  register(name: String, email: String!, password: String!): User
}
