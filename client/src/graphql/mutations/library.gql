mutation ADD_BOOK($book: BookInput) {
  addBook(book: $book) {
    id
    title
    author
  }
}

# mutation ADD_COMMENT($message: MessageInput!) {
#   addMessage(message: $message) {
#     id
#     text
#   }
# }

mutation ADD_TO_CART($book: BookInput) {
  addToCart(book: $book) @client {
    id
    title
    author
    count
  }
}
